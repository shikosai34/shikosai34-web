# 第34回 茨香祭HP 要件定義 — 概要・システム境界

← [01-introduction.md](01-introduction.md)（文書構成・未決定事項） ／ → [03-design.md](03-design.md)

## 目次

- [第34回 茨香祭HP 要件定義 — 概要・システム境界](#第34回-茨香祭hp-要件定義--概要システム境界)
  - [目次](#目次)
  - [2.2 想定利用者](#22-想定利用者)
  - [2.3 前提条件](#23-前提条件)
  - [2.4 技術スタック](#24-技術スタック)
  - [2.5 HP と FesFlow の役割分担](#25-hp-と-fesflow-の役割分担)
  - [2.6 データの公開範囲](#26-データの公開範囲)
  - [2.7 ページ一覧](#27-ページ一覧)
  - [2.8 各ページとデータモデルの対応](#28-各ページとデータモデルの対応)
    - [2.8.1 `/circle` サークル一覧](#281-circle-サークル一覧)
    - [2.8.2 `/circle/[slug]` サークル詳細](#282-circleslug-サークル詳細)
      - [slug の値について](#slug-の値について)
      - [FesFlow 未導入サークルの表示](#fesflow-未導入サークルの表示)
    - [2.8.3 `/event` `/event/[slug]` ステージ企画](#283-event-eventslug-ステージ企画)
    - [2.8.4 `/timetable` タイムテーブル](#284-timetable-タイムテーブル)
    - [2.8.5 `/map` 会場マップ](#285-map-会場マップ)
    - [2.8.6 `/access` アクセス](#286-access-アクセス)
    - [2.8.7 `/news` お知らせ](#287-news-お知らせ)
    - [2.8.8 `/goods` グッズ ［未定］](#288-goods-グッズ-未定)
    - [2.8.9 `/decoration` 装飾 ［未定］](#289-decoration-装飾-未定)
    - [2.8.10 `/sponsor` 協賛](#2810-sponsor-協賛)
    - [2.8.11 `/faq` よくある質問 ［要検討］](#2811-faq-よくある質問-要検討)
  - [2.9 コンテンツの管理方法](#29-コンテンツの管理方法)
    - [2.9.1 コレクションの構成](#291-コレクションの構成)
    - [2.9.2 更新の手順](#292-更新の手順)
    - [2.9.3 原稿の反映](#293-原稿の反映)
  - [2.10 機能要件一覧](#210-機能要件一覧)

## 2.2 想定利用者

- **一般来場者**（地域住民・近隣校の生徒とその保護者）
  - 主な利用目的: 開催日時、アクセス、企画内容の確認
  - 主な利用環境: スマートフォン
- **在校生・受験検討者**
  - 主な利用目的: タイムテーブル、企画詳細の確認
  - 主な利用環境: スマートフォン
- **出展団体**（サークル・クラス）
  - 主な利用目的: 自団体の掲載内容の確認
  - 主な利用環境: スマートフォン／PC
- **協賛企業**
  - 主な利用目的: 自社ロゴの掲載確認
  - 主な利用環境: PC

**スマートフォンでの閲覧を第一の設計基準とする。** 利用者の多くを占める一般来場者・在校生がスマートフォンで閲覧するため。Figma のアートボード作成もモバイル幅から着手する。

## 2.3 前提条件

- **テーマ**: 守破離
- **デザインキーワード**: 和風 × サイバーパンク
- **ページ構成（URL一覧）**: 17件。第2.7節
- **FesFlow の公開URL**: HP からのリンク先として必要

## 2.4 技術スタック

第33回リポジトリの構成をそのまま採用する。新規に技術選定を行わない理由は、前年度の実装を参照できること、および学習コストを抑えて内容の制作に時間を配分できることによる。

- **Bun**（ランタイム／パッケージ管理）: 依存関係の管理、ユニットテストの実行
- **proto**（バージョン管理・ツールチェーン）: Bun 等のバージョン統一
- **Astro**（フレームワーク）: 静的サイト生成
- **Tailwind CSS**（CSS）: デザイントークンによるスタイル定義
- **Biome**（Linter / Formatter）: コード品質の統一
- **lefthook**（Git フック）: コミット前のチェック実行
- **Cloudflare**（ホスティング、Wrangler 経由）: 本番配信
- **Dev Container / Docker / GitHub Codespaces**（開発環境）: 環境差異の吸収

**参考:** FesFlow 側は Cloudflare Pages / Cloudflare Tunnel、API は Hono で構築する構想である（システム構想記事より）。HP と同じ Cloudflare 上に配置されるため、ドメイン設計を両者で調整する必要がある。

## 2.5 HP と FesFlow の役割分担

茨香祭のデジタル基盤は次の2つで構成される。

- **HP（本プロジェクト）**
  - 役割: 開催情報・出展情報の**閲覧**
  - 性質: 静的サイト。ログイン不要、個人情報を収集しない
- **FesFlow**
  - 役割: 購入・体験に関わる**取引**
  - 性質: 動的システム。来場者のマイページ、サークル向けPOS

**原則: HP は取引に関わる機能を実装しない。該当する機能は FesFlow へリンクする。**

## 2.6 データの公開範囲

| データ | HP掲載 | 担当システム | 根拠・備考 |
| --- | --- | --- | --- |
| サークル情報（団体名・紹介文・場所・画像） | **する** | HP | 別途 Google フォーム等で収集し HP に掲載する |
| ステージ企画情報（event） | **する** | HP | `/event` はステージ企画を指す |
| タイムテーブル | **する** | HP | |
| 会場マップ | **する** | HP | |
| 協賛企業情報 | **する** | HP | |
| 実行委員情報 | **する** | HP | |
| お知らせ | **する** | HP | |
| グッズ情報 | **する** | HP | 掲載項目は ［未定］。第2.8.8節 |
| 装飾情報 | **する** | HP | 掲載項目は ［未定］。第2.8.9節 |
| メニュー | **しない** | FesFlow | 「そもそもメニューも出さない」 |
| メニュー価格 | **しない** | FesFlow | 掲載可否に食い違いがあり ［要確認］ |
| トッピング | **しない** | FesFlow | |
| レビュー | **しない** | FesFlow | FesFlow 導入サークルのみ、購入履歴に基づく権限制御が必要なため |
| 整理券 | **しない** | FesFlow | |
| 待ち時間の目安 | **しない** | FesFlow | |
| 事前オーダー | **しない** | FesFlow | |
| スタンプラリー | **しない** | FesFlow | |
| 抽選 | **しない** | FesFlow | |
| 売上・POS | **しない** | FesFlow | 対来場者の公開情報ではない |
| 動画 | **対象外** | — | 該当する構想が確認できないため範囲から除外する |
| アレルギー情報 | **専用項目としては設けない** | — | 紙での掲示で対応する。必要な場合はサークル紹介文に記述してもらう |

## 2.7 ページ一覧

| # | パス | ページ名 | 概要 |
| --- | --- | --- | --- |
| 1 | `/` | トップ | 開催概要と各ページへの導線 |
| 2 | `/news` | お知らせ | 更新情報の時系列一覧 |
| 3 | `/about` | 茨香祭について | 開催概要、実行委員会の説明 |
| 4 | `/theme` | テーマ | 「守破離」の説明 |
| 5 | `/circle` | サークル一覧 | 出展団体の一覧 |
| 6 | `/circle/[slug]` | サークル詳細 | 団体ごとの詳細。FesFlow への導線を含む |
| 7 | `/event` | ステージ企画一覧 | ステージ企画の一覧 |
| 8 | `/event/[slug]` | ステージ企画詳細 | 企画ごとの詳細 |
| 9 | `/timetable` | タイムテーブル | 日程別・会場別の時間割 |
| 10 | `/map` | 会場マップ | 校内の配置図 |
| 11 | `/access` | アクセス | 交通手段、駐車場 |
| 12 | `/goods` | グッズ | 販売グッズの紹介 |
| 13 | `/decoration` | 装飾 | 校内装飾の紹介 |
| 14 | `/staff` | 実行委員紹介 | 部署構成と紹介 |
| 15 | `/sponsor` | 協賛 | 協賛企業のロゴ一覧 |
| 16 | `/faq` | よくある質問 | |
| 17 | `/cautions` | 注意事項 | 来場時の注意 |
| — | `/404` | 404 | Issue の一覧には未記載だが実装上必要 |

## 2.8 各ページとデータモデルの対応

| パス | 参照するデータ | データソース | FesFlowへのリンク |
| --- | --- | --- | --- |
| `/` | 開催概要、お知らせ（新着数件）、サークル（抜粋）、ステージ企画（抜粋）、協賛 | HP内 | あり（FesFlow 全体への導線） |
| `/news` | お知らせ | HP内 | なし |
| `/about` | 静的テキスト | HP内 | なし |
| `/theme` | 静的テキスト | HP内 | なし |
| `/circle` | サークル情報（一覧） | Googleフォーム → 確認 → Content Collections | なし |
| `/circle/[slug]` | サークル情報（詳細） | Googleフォーム → 確認 → Content Collections | **あり**（該当サークルの FesFlow ページ） |
| `/event` | ステージ企画情報（一覧） | HP内データ | なし |
| `/event/[slug]` | ステージ企画情報（詳細） | HP内データ | ［要検討］ |
| `/timetable` | ステージ企画情報、サークル情報の実施時間 | HP内データ | なし |
| `/map` | 会場配置データ | HP内データ（画像＋補足テキスト） | なし |
| `/access` | 静的テキスト、外部地図 | HP内 | なし |
| `/goods` | グッズ情報 | HP内データ | ［未定］ |
| `/decoration` | 装飾情報 | HP内データ | なし |
| `/staff` | 実行委員情報 | HP内データ | なし |
| `/sponsor` | 協賛企業情報 | HP内データ | なし |
| `/faq` | 質問・回答 | HP内データ | ［要検討］（FesFlow の使い方に関する質問を含めるか） |
| `/cautions` | 静的テキスト | HP内 | なし |

### 2.8.1 `/circle` サークル一覧

| 項目 | 内容 |
| --- | --- |
| 表示項目 | 団体名、サムネイル画像、カテゴリ、実施場所 |
| 並び順 | ［要検討］ |
| 絞り込み | カテゴリによる絞り込み |
| 分類方法 | ［未定］。学科別／出展形態別／場所別のいずれか |

### 2.8.2 `/circle/[slug]` サークル詳細

**この画面が HP と FesFlow の接続点であり、設計上もっとも検討を要する。**

| 項目 | 掲載 | 備考 |
| --- | --- | --- |
| 団体名 | する | |
| 紹介文 | する | アレルギーに関する記述が必要な場合はここに含める |
| 画像 | する | 枚数の上限は ［未定］ |
| 実施場所 | する | `/map` の該当箇所へリンクする ［要検討］ |
| 実施時間 | する | |
| カテゴリ | する | |
| SNSリンク | する | 任意項目 |
| **FesFlow へのリンク** | する | **FesFlow 導入サークルのみ** |
| メニュー | しない | FesFlow 側で表示 |
| 価格 | しない | 掲載可否に食い違いがあり ［要確認］ |
| レビュー | しない | FesFlow 側で表示 |
| 待ち時間 | しない | FesFlow 側で表示 |

#### slug の値について

Issue のコメントに「Slug はそれぞれのサークル名が入り」とある。サークル名をそのまま slug に用いる場合、日本語を含む URL となる。次の問題が生じる。

- URL がパーセントエンコードされ、`/circle/%E3%81%82...` のような文字列になる。SNS で共有した際に判読できない。
- 団体名に記号（`&`、`/`、空白など）が含まれる場合、URL として使用できず個別の変換規則が必要になる。
- 団体名が変更された場合、URL が変わり既存のリンクが無効になる。

**代案:** サークルごとに英数字の識別子（例: `/circle/robot-club`、または連番 `/circle/a01`）を設定し、ページ内に日本語の団体名を表示する。識別子は原稿収集フォームで各団体に入力してもらうか、実行委員会側で採番する。**この点は Figma のデザイン作成前に決定する必要がある**（URL がページ設計に影響するため）。

#### FesFlow 未導入サークルの表示

FesFlow の導入は全サークル一律ではない（レビュー機能が「FesFlow 導入サークルのみ可能」とされているため）。したがって `/circle/[slug]` には次の2つの状態が存在する。

| 状態 | 表示 |
| --- | --- |
| FesFlow 導入 | FesFlow へのリンクを表示する |
| FesFlow 未導入 | リンク領域を表示しない、または「当日は現地でのお受け取りとなります」等の代替文言を表示する |

**Figma では両方の状態をデザインする必要がある。** 片方のみを作成すると実装時に判断が必要になる。

### 2.8.3 `/event` `/event/[slug]` ステージ企画

Issue の確認事項「`/event` と `/circle` の関係」に対する回答は「event はステージ企画を想定」である。したがって次のとおり整理する ［確定］。

| 対象 | 掲載先 |
| --- | --- |
| 常設の出展（模擬店、展示、体験など） | `/circle` |
| ステージ上で時間を区切って実施する企画（演奏、パフォーマンス、競技など） | `/event` |

**［要検討］** 同一の団体がサークル出展とステージ出演の両方を行う場合、`/circle/[slug]` と `/event/[slug]` の相互リンクを設けるかを決める。

### 2.8.4 `/timetable` タイムテーブル

| 項目 | 内容 |
| --- | --- |
| 表示軸 | 日付（2日間）× 会場 × 時刻 |
| 表示形式（PC） | 縦軸を時刻、横軸を会場とする表形式 |
| 表示形式（モバイル） | 会場ごとに切り替える縦一列のリスト形式 |
| 掲載情報 | 開始時刻、終了時刻、企画名、実施団体、会場 |
| 詳細への導線 | `/event/[slug]` へリンクする |

**留意点:** 直前の変更が発生しやすい情報である。第2.9節の手順に従い、開催直前にも更新できる体制を確保する。

### 2.8.5 `/map` 会場マップ

| 項目 | 内容 |
| --- | --- |
| 掲載情報 | 校内の建物・教室の配置、各サークルの出展場所、ステージ位置、トイレ・救護室・喫煙所などの設備 |
| 形式 | ［要検討］。画像1枚、SVG による領域指定、または階層別の複数画像 |
| サークルとの連携 | 出展場所から `/circle/[slug]` へリンクできるか ［要検討］ |

**留意点:** モバイルでの閲覧が中心となるため、画像1枚を縮小表示すると判読できない。拡大表示の手段（ピンチ操作の許容、階層別の分割表示）を設計時に決める。

### 2.8.6 `/access` アクセス

| 項目 | 内容 |
| --- | --- |
| 掲載情報 | 所在地、公共交通機関、自動車、駐車場、自転車 |
| 地図 | Google Maps の埋め込み。読み込みは遅延させる |

`/map`（校内配置）との役割の違いを、各ページの冒頭で明示する。利用者が混同しやすい。

### 2.8.7 `/news` お知らせ

| 項目 | 内容 |
| --- | --- |
| 掲載情報 | 公開日、見出し、本文、カテゴリ |
| 並び順 | 公開日の降順 |
| トップへの表示 | 新着数件を `/` に表示する |

**留意点:** 開催直前に更新頻度が上がる。第2.9節のとおり Content Collections で管理するため、更新にはコミットとデプロイを伴う。複数名が手順を実行できる状態にしておく。

### 2.8.8 `/goods` グッズ ［未定］

掲載項目が未定である。次のいずれかを決める必要がある。

- 商品名、画像、説明のみを掲載する（価格を含めない）
- 価格を含めて掲載する
- 販売場所・販売時間を含めるか

**第2.8.2節の価格に関する［要確認］と連動する。** サークルのメニュー価格を掲載しない方針であるにもかかわらず、公式グッズの価格を掲載するのは、来場者から見て一貫性を欠く可能性がある。両者の方針を合わせて決定する。

### 2.8.9 `/decoration` 装飾 ［未定］

掲載目的が未定である。次のいずれかを確認する。

- 当日の装飾を事前に紹介し、来場の動機付けとする
- 装飾の制作過程を記録として公開する
- 開催後に記録として掲載する（公開時点では空になる）

3番目の場合、公開時に内容が存在しないため、ページの初期状態の設計が必要になる。

### 2.8.10 `/sponsor` 協賛

| 項目 | 内容 |
| --- | --- |
| 掲載情報 | 企業名、ロゴ画像、企業サイトへのリンク |
| 区分 | 協賛金額による区分がある場合はロゴ表示サイズを変える ［未定］ |
| 掲載順 | 区分内は五十音順を想定 ［要検討］ |

**注意事項:** 協賛企業のロゴは提供されたデータをそのまま使用し、色の変更・変形・トリミングを行わない。デザイントンマナのアクセントカラーを重ねる加工も行わない。企業側のロゴ利用規定に抵触する可能性がある。

### 2.8.11 `/faq` よくある質問 ［要検討］

FesFlow の利用方法に関する質問（マイページの登録方法、整理券の取得方法など）を HP の FAQ に含めるか、FesFlow 側に置くかを決める。両方に分散すると、更新時に片方が古くなる。

## 2.9 コンテンツの管理方法

**Astro Content Collections を全コンテンツに適用する。** Markdown / JSON をリポジトリで管理し、ビルド時に型検証を行う。第33回と同じ方法であるため追加の学習が不要であり、管理方法をコンテンツ種別ごとに分けないことで、更新手順を1つに統一できる。

### 2.9.1 コレクションの構成

`src/content/` 以下に、コンテンツ種別ごとのコレクションを置く。

- `news` — お知らせ。Markdown（本文を持つため）
- `circles` — サークル情報。JSON
- `events` — ステージ企画情報。JSON
- `timetable` — タイムテーブル。JSON
- `sponsors` — 協賛企業情報。JSON
- `staff` — 実行委員情報。JSON
- `goods` — グッズ情報。JSON
- `decoration` — 装飾情報。JSON
- `faq` — 質問・回答。JSON

各コレクションには `src/content.config.ts` でスキーマ（Zod）を定義し、必須項目の欠落や型の誤りをビルド時に検出する。`/about`、`/theme`、`/access`、`/cautions` のように単一ページで内容が固定されるものは、コレクションとせずページ内に直接記述する。

### 2.9.2 更新の手順

1. 対象のファイルを編集する（新規のお知らせであれば `src/content/news/` にファイルを追加する）。
2. `docs/<内容>` ブランチで Pull Request を作成する。
3. レビュー承認後に `main` へマージする。
4. Cloudflare へ自動デプロイされる。

**公開日の扱い:** お知らせなど公開日を持つコンテンツには `publishedAt` を必須項目とし、並び順に用いる。将来の日付を指定した項目をビルド時に除外するかは実装時に決める。

### 2.9.3 原稿の反映

サークル情報は Google フォームで各団体から収集する。**回答をビルド時に直接読み込まず、確認を経てから手作業で JSON へ反映する。** フォームの回答をそのまま公開すると、誤字や不適切な記述、画像形式の不統一がそのまま反映されるため、掲載内容の確認を挟む。あわせて、紹介文に個人名・連絡先が含まれないことを確認する（第4.5節）。

**収集する項目:** `circles` コレクションのスキーマは次を前提とする。

| 項目 | 必須 | 備考 |
| --- | --- | --- |
| 団体名 | 必須 | |
| 団体名（かな） | 必須 | 並び順に使用 |
| URL識別子 | 必須 | 第2.8.2節の slug。英数字とハイフンのみ |
| 紹介文 | 必須 | 文字数の上限を指定する |
| カテゴリ | 必須 | 選択式 |
| 実施場所 | 必須 | |
| 実施時間 | 必須 | |
| FesFlow 導入予定 | 必須 | リンク表示の判定に使用 |
| 画像 | **任意** | 縦横比・解像度・形式を指定して提出させる |
| SNSリンク | 任意 | |

**画像が任意であることの影響:** 画像のないサークルが存在するため、一覧・詳細の両方で代替表示が必要になる（第3.7.1節の `Image/Placeholder`）。また提出形式を指定しないと縦横比が揃わず、加工作業が発生する。

## 2.10 機能要件一覧

画面上の動作を伴う機能を列挙する。**Figma では第3.7.2節のとおり、動作前後の両方の状態を作成する。**

| # | 機能 | 内容 |
| --- | --- | --- |
| F-01 | ページ間ナビゲーション | ヘッダー・フッターからの遷移。17ページを整理して提示する必要がある |
| F-02 | モバイル用メニュー | 開閉動作。JavaScript は最小限に留める |
| F-03 | FesFlow への遷移 | `/circle/[slug]` およびトップから。導入・未導入の2状態に対応する |
| F-04 | サークルのカテゴリ絞り込み | `/circle` での絞り込み |
| F-05 | タイムテーブルの日付切り替え | 1日目／2日目の切り替え |
| F-06 | 地図の埋め込み | `/access` に Google Maps。読み込みは遅延させる |
| F-07 | 会場マップの拡大表示 | `/map` での拡大操作 |
| F-08 | OGP 画像の出力 | ページごとの OGP 設定 |
| F-09 | サイトマップ生成 | `@astrojs/sitemap` を利用 |

**方針:** 静的サイトとして構築し、サーバー側の処理を必要とする機能（フォーム送信、ログイン、動的な検索）は実装しない。該当する機能は FesFlow または外部サービスへ誘導する。**サイト内検索は実施しない**（17ページ規模では必要性が低い）。

**ナビゲーションについての留意点:** 17ページはヘッダーに並べるには多い。第33回は4項目程度であった。階層化（例: 「企画」の下に `/circle` `/event` `/timetable`、「来場案内」の下に `/map` `/access` `/cautions` `/faq`）が必要になる。**この分類は Figma のワイヤーフレーム作成前に決定する**（第3.0節の1）。
